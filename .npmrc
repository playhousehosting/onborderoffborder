# NPM Security Configuration

# Use exact versions (no caret ranges)
save-exact=true

# Require confirmation for npm publish
publish-access=restricted

# Audit settings
audit-level=moderate

# Legacy peer deps (if needed for React compatibility)
legacy-peer-deps=false
